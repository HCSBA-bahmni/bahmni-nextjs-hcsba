import { describe,expect,it } from "vitest";import { runConfiguredValidator,validateChileRun } from "./validators";
describe("validation adapters",()=>{it("validates Chilean RUN",()=>{expect(validateChileRun("12.345.678-5")).toBe(true);expect(validateChileRun("12.345.678-9")).toBe(false)});it("fails closed for remote validators",()=>{expect(runConfiguredValidator("evalRemoteJs","x")).toEqual({valid:false,message:"Validador no soportado: evalRemoteJs"})})});
describe("configured field rules",()=>{it("enforces required fields",()=>expect(runConfiguredValidator("required"," ").valid).toBe(false));it("uses configured regular expressions",()=>expect(runConfiguredValidator("regex","ana@example.org",{pattern:"^[^@]+@[^@]+$"}).valid).toBe(true));it("enforces minimum length",()=>expect(runConfiguredValidator("minLength","abc",{length:4}).valid).toBe(false));it("accepts a RUN with K verifier",()=>expect(validateChileRun("1.000.005-K")).toBe(true))});
